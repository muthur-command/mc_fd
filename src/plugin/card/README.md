# 卡片管理插件（前端）

- **位置**：`src/plugin/card/`
- **路由**：由 `src/pages/plugin/card.vue` 挂载，路径 `/plugin/card`
- **后端**：MC 插件 `backend/plugin/card`，API `/api/v1/cards`

## 目录

- `api/index.ts` - 卡片 API（getCardListApi、CardItem）
- `views/index.vue` - 卡片管理页视图
