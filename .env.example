# API：开发时留空可走 Vite 代理(见 vite.config server.proxy)，避免 CORS
# 留空时请求发往同源 /api，由代理转发到 API_PROXY_TARGET
# 生产环境填后端完整地址
VITE_SERVER_API_URL=
VITE_SERVER_API_PREFIX=/api
VITE_SERVER_API_TIMEOUT=5000
API_PROXY_TARGET=http://localhost:8000